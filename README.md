### `Installation and Setup Instructions`

Clone down this repository. 

Change to backend

Installation:

`npm install`

To Start Server:

`npm start`

Then in A new terminal open FrontEnd

Installation:

`npm install`

To Start Server:

`npm start`

To Visit App:

`localhost:3000`
